# 竞研台迭代记录

## 2026-09-10 · 新项目初始化

- 不修改原项目，在独立目录创建“竞研台”。
- 首页只围绕竞品分析，不包含原有方法 Demo 或微生物业务内容。
- 历史记录使用用户可读摘要，不展示 JSON 或内部对象结构。
- 配置服务端 Tavily 入口；没有 Key 时使用本地演示资料。

## 2026-09-10 · 文件驱动分析

- 移除产品类别、竞品名称和固定指标的手动输入。
- 新增 Excel、CSV、PDF 多文件上传与哈希记录。
- Excel 读取全部工作表，并保留工作表名、表头、行数和行位置。
- PDF 按页提取正文，保留页码、空页状态和页面片段。
- 根据文件字段自动识别品牌/产品、产品类别和比较维度，并显示置信度。
- 将文件内已有事实与外部搜索分开；联网只搜索每个品牌的缺失维度。
- 记录跳过的重复搜索、跳过理由和实际生成的查询词。
- 分析结果升级为一页结论、多指标矩阵、待核验项、机会风险和来源记录。
- 新增证据覆盖热图和六维证据图。图表展示资料完整度，不冒充产品能力评分。
- 评估 R 后端后，为避免不同部署环境的 R 运行时与中文字体差异，最终统一使用服务端 Python/matplotlib 绘图；用户端无需安装绘图环境。
- 新增 Markdown 报告、证据 JSON 和比较 CSV 下载。
- 修复 Markdown 导出依赖可选 `tabulate` 导致页面报错的问题。
- 增加实验室、分析、测量仪器和医疗器械类别识别，并为仪器资料增加范围/精度、检测能力、规格配置、售后合规等维度。
- 上传后增加服务端 AI 文件识别阶段（固定 Prompt `file-intake-v1`）：提取产品类型、品牌、搜索关键词、重点数据、比较维度和资料缺口；AI 不可用时明确降级为规则识别。
- 将文件识别 Prompt 升级为通用 `file-intake-v2`：支持实体关系、字段单位/条件、来源位置、比较维度理由、搜索计划、冲突和质量字段，不再按仪器/软件二分。
- 搜索层增加 Tavily、Brave Search、Exa、SerpAPI 适配器；单个任务只选择一个供应商，按服务端配置自动选择，避免重复付费。

## 验证

- Python 编译检查通过。
- 演示资料解析、缺失维度查询生成和证据矩阵检查通过。
- 浏览器完成“开始生成分析 -> 分析结果”流程。
- 热图与六维图均在真实 Streamlit 页面渲染。

## 下一轮

- 接入网页正文读取与事实级抽取，搜索摘要继续只用于发现。
- 增加官方来源优先级、URL 缓存、内容哈希去重和冲突记录。
- 增加 PDF 表格识别与非预置品牌的实体识别。

## 2026-09-10 · 首版多来源分析与报告增强

### 已完成

- 保留服务端 Python 热图和六维证据图；停止为每次页面分析自动生成未使用的大体积 TIFF/PDF/SVG，避免磁盘快速增长。
- 增加“上传产品资料”和“输入产品名称”两个入口，以及国内电商/海外电商市场选择。
- 无文件模式在搜索前先调用千问结构化识别产品身份、类别、比较维度和检索计划；失败时明确显示规则降级。
- 文件分析默认改为千问官方百炼兼容接口，Prompt 继续使用 `file-intake-v2`；产品名入口使用 `product-name-intake-v1`。
- 默认让 Qwen 与 Tavily 执行同一批高优先级搜索，按 canonical URL 和内容哈希合并来源并保留全部发现工具。
- 当去重后来源或候选竞品不足时，按配置有限调用 Brave、Exa、SerpAPI。
- 增加任务搜索次数上限、外部页面读取上限、搜索/页面缓存命中、失败调用、剩余次数和成本估算。
- 外部来源先读取网页正文或 Jina Reader，再由千问抽取事实；没有 URL 的搜索回答、无法访问页面和搜索摘要不会生成已核验事实。
- 商品表增加品牌、产品、型号、SKU/货号、平台、地区、币种、价格、规格、评分、评论数和评论摘要。
- 分析结果增加关键差异与资料缺口、数据驱动的下一步建议、人工确认队列和更完整的 Markdown 报告。
- 修正候选实体边界：`possible_competitor` 和 `substitute` 不再直接混入文件内正式品牌。
- 历史摘要增加入口、市场、搜索次数、去重后来源和待确认数量，并通过兼容迁移保留旧记录可读。
- 修复搜索缓存被页面缓存统计覆盖的问题，以及大写 URL 协议无法去重的问题。

### 已验证

- `python -m py_compile app.py tests\\test_app.py` 通过。
- 9 项标准库单元测试全部通过。
- Mock 验证 Qwen + Tavily 对同一 URL 合并为一个来源，并保留 `qwen`、`tavily` 两个发现工具。
- Mock 验证覆盖足够时不调用备用工具，覆盖不足时触发 Brave。
- 验证没有 URL 的 Qwen 回答不会进入来源台账。
- CSV 商品字段、无文件入口、证据矩阵和报告结构回归通过。
- Playwright 完成 1440px 桌面和 390px 窄屏检查；页面无横向溢出，图表均加载完成，浏览器控制台无错误或警告。
- 使用现有生化培养箱 Excel 与移液器 PDF 完成多文件实测：读取 1 个工作表和 4 页 PDF，生成 4 个比较对象、5 个自动维度和 6 条来源。
- 浏览器验收发现并修复空结果页误渲染 Streamlit 内部组件说明，以及顶部工具栏遮挡品牌名的问题。

### 待补充

- 使用千问官方真实 Key 验证 `enable_search` 的实际响应结构、成本和延迟。
- 使用真实 Tavily/Brave/Exa/SerpAPI Key 记录固定国内/海外任务的覆盖差异。
- 扫描型 PDF OCR、人工确认队列持久化、定期复查和版本差异留待后续迭代。
